import React from 'react';
import { NavBar, Button } from 'antd-mobile'

const Example = () => {
    return (
        <div>
            <NavBar>Example</NavBar>
            <Button loading style={{ margin: '30px' }}>Example</Button>
        </div>
    );
};

Example.propTypes = {

};

export default Example;
